<div class="nested-fields">
	<div class="row">
		<%= f.grouped_collection_select :jig_id, Customer.order(:name), :jigs, :name, :id, :name, { include_blank: 'Select Jig' }, class: "form-control jig_select col-xs-3" %>
		<%= f.input :repaired, class: 'col-xs-3' %>
		<%= f.input :cleaned, class: 'col-xs-3' %>
		<%= link_to_remove_association "remove jig", f %>
	</div>
</div>
