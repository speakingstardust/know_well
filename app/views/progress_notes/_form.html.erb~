<div class="modal-dialog">
  <div class="modal-content">
    <% if action_name == "new" %>
      <%= simple_form_for @progress_note, url: project_progress_notes_path, remote: true, html: { style: "display:inline;" } do |f| %>
    <% else %>
      <%= simple_form_for @progress_note, url: edit_project_progress_notes_path(@progress_note.project.id, @progress_note.id), remote: true, html: { style: "display:inline;" } do |f| %>
    <% end %>
      <div class="modal-body">
        <ul class="errors"></ul>

        <%= f.hidden_field :project_id, value: @project.id %>

        <%= f.input :note %>

      </div>
      <div class="modal-footer">
        <%= f.submit class: "btn btn-primary" %>
        <%= link_to "Cancel", "#", class: "btn", data: { dismiss: "modal" } %>
      </div>
    <% end %>
  </div>
</div>
