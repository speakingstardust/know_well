$(".jig_work_orders.edit").ready ->
  add_fields = $(".add_fields")
  last_button = add_fields.last()
  last_button.toggleClass("add_fields")
  add_fields = $(".add_fields")
  add_fields.remove()
  last_button.toggleClass("add_fields")
